{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Serena: Reindex WordFilterPlus",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-ExecutionPolicy",
                "Bypass",
                "-File",
                "${workspaceFolder}\\.tools\\reindex_serena.ps1"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build"
        }
    ]
}
